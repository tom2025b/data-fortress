# Makes dashboard/utils/ a Python package.
# An __init__.py file (even empty) tells Python that this directory is a package,
# which allows other files to import from it with: from utils.db import get_stats
